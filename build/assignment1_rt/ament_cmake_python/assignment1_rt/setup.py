from setuptools import find_packages
from setuptools import setup

setup(
    name='assignment1_rt',
    version='0.0.0',
    packages=find_packages(
        include=('assignment1_rt', 'assignment1_rt.*')),
)
